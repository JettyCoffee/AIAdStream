# MainActor 隔离与默认参数

## 背景
Swift 并发模型默认将部分静态成员置于 MainActor 隔离域内。

## 问题
当 MainActor 隔离的静态属性作为函数默认参数时，会触发隔离警告，影响可维护性与可预期性。

## 处理方案
- 避免将 MainActor 隔离的静态属性直接作为默认参数。
- 在调用处显式传参，确保并发语义清晰。

## 设计要点
- 以调用处显式传参为主，降低并发隐式依赖。
- 减少默认参数对隔离域的耦合，提升可读性。
